# SAE11_2022 : Jeu de paires

Commandes :

- `make` & `make build` : compile le projet,
- `make run` : compile le projet et lance le jeu.
- `make clean` : supprime tous les fichiers liés à la compilation.